const queries = ['integrate-ai', 'test'];

expect(result['integrate-ai'].channels).toHaveLength(1);
expect(result['integrate-ai'].channels[0].name).toBe('Integrate AI');

const queries = ['integrate-ai', 'test'];

expect(result['integrate-ai'].channels).toHaveLength(1);

const queries = ['integrate-ai', 'test'];

expect(result['integrate-ai'].channels).toHaveLength(1); 